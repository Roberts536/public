/*
Key scheduler for 128-bit AES encryption.
Generates the next 128-bit subkey.
*/

#include <iostream>
#include <bitset>

// Function expects a pointer to an array of four 32b words
void get_next_subkey(std::bitset<32> input_key[4])
{
	// We then calculate each word in the next subkey


	// Return the final subkey
}


